# @package _global_

defaults:
  - override /datamodule: emnist.yaml
  - override /feature_extractor: mlp.yaml
  - override /transforms:
      [
        to_tensor.yaml,
        grey_normalize.yaml,
        random_horizontal_flip.yaml,
        random_rotation.yaml,
      ]
