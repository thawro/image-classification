# @package _global_

defaults:
  - override /datamodule: mnist.yaml
  - override /feature_extractor: mlp.yaml
  - override /transforms: [permute.yaml, grey_normalize.yaml, rotation.yaml]
  - override /callbacks: default.yaml
  - override /head: default.yaml
  - override /hydra: default.yaml
  - override /model: default.yaml

run_name: test

transforms:
  rotation:
    degrees: 10

datamodule:
  batch_size: 128
